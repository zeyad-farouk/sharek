/**
 * @deprecated — Import from './authMiddleware' instead.
 *
 * Backward-compatible shim so existing route files (cart, item, message, etc.)
 * do not need to be updated until a future refactor.
 */
module.exports = require('./authMiddleware');
